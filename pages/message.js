import React from 'react';
import { Breadcrumb, MessageContent } from '@components/index';

const Message = () => {
    return (
        <>
            <Breadcrumb title="Message" />
            <MessageContent />
        </>
    );
};

export default Message;
