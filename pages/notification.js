import React from 'react';
import { Breadcrumb, NotificationContent } from '@components/index';

const Notification = () => {
    return (
        <>
            <Breadcrumb title="Notification" />
            <NotificationContent />
        </>
    );
};

export default Notification;
